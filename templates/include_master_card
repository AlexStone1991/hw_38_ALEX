<div class="col">
    <div class="card h-100 shadow-sm simpsons-card">
        {% if master.photo %}
        <img src="{{ master.photo.url }}" class="card-img-top" alt="{{ master.name }}" style="height: 200px; object-fit: cover;">
        {% else %}
        <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height: 200px;">
            <i class="bi bi-person-badge fs-1 text-muted"></i>
        </div>
        {% endif %}
        <div class="card-body">
            <h5 class="card-title" style="color: #1D6FC4;">{{ master.name }}</h5>
            <p class="card-text">
                <i class="bi bi-star-fill" style="color: #FFD90F;"></i> 
                <span style="color: #1D6FC4;">Опыт: {{ master.experience }} лет</span>
            </p>
            <div class="d-flex justify-content-between">
                <a href="{% url 'master_detail' master.id %}" class="btn btn-simpsons-outline">
                    <i class="bi bi-info-circle"></i> Подробнее
                </a>
                <button class="btn btn-simpsons" data-bs-toggle="modal" data-bs-target="#orderModal">
                    <i class="bi bi-calendar-plus"></i> Записаться
                </button>
            </div>
        </div>
    </div>
</div>